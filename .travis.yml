compiler: gcc

os: linux

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake
      - gcc-8
      - libinput-dev
      - zlib1g-dev

script:
  - cmake .
  - make -j$(nproc)
